meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/logout
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("should return 204 on success", function() {
    expect(res.status).toBe(204);
  });
}
