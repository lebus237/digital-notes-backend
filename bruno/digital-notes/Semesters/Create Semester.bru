meta {
  name: Create Semester
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v1/admin/semesters
  body: json
  auth: inherit
}

body:json {
  {
    "name": "First Semester"
  }
}

tests {
  test("should return 201 on success", function() {
    expect(res.status).toBe(201);
  });

  test("should return created id", function() {
    expect(res.body.id).toBeTruthy();
  });
}
